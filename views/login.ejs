<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs") %>
    <body>
        <div>
            <div class="form-container">
                <h2> Login </h2>
                <form id="login" action="/login" method="post">
                    <div class="form-group">
                        <label for="email"> Email: </label>
                        <input type="text" id="email" value="<%= emailVal %>" class="<%= emailErrMsg ? 'inputErr' : '' %>" id="email-input" name="email" placeholder="Enter your email" required autocomplete="off">
                        <p id="email-err" class="form-err"> <%= emailErrMsg %> </p>
                    </div>
                    <div class="form-group">
                        <label for="password"> Password: </label>
                        <input type="text" id="password" value="<%= passwordVal %>" name="password" placeholder="Password" required autocomplete="off">
                        <p id="email-err" class="form-err"> <%= passwordErrMsg %> </p>
                    </div>
                    <button type="submit" class="submit-btn">Log In</button>
                </form>
                <p class="subtext"> Don't have an account? <a href="/signup">Sign Up</a></p>
            </div>
        </div>
    </body>
</html>